import { NonSelectableCardComponent } from './non-selectable-card.component'

describe('NonSelectableCardComponent', () => {
  it('should mount', () => {
    cy.mount(NonSelectableCardComponent)
  })
})